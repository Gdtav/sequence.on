<%= form_with(model: sheet, local: true) do |form| %>
  <% if sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sheet.errors.count, "error") %> prohibited this sheet from being saved:</h2>

      <ul>
      <% sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bpm %>
    <%= form.number_field :bpm %>
  </div>

  <div class="field">
    <%= form.label :instrument %>
    <%= form.number_field :instrument %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
